:8081 {
    root * /var/www/backend/public
    encode gzip

    @api path /api/*
    handle @api {
        php_fastcgi 127.0.0.1:9000
    }

    handle {
        try_files {path} {path}/ /index.html
        file_server
    }
}
